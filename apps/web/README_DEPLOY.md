# Deploy Web

## Environment variables
- `VITE_API_URL` – your API base URL (e.g. https://diary-api.onrender.com)

## Build
```bash
npm install
npm run build
```

Deploy the `dist/` output to Vercel/Netlify, or run `npm run preview` to test locally.
